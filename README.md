# wb_service

**Тестовый сервис для взаимодействия с WB_API песочницей**  

---

## 📂 Структура проекта

src/
├─ api/
│ └─ api.py # Endpoint запроса ABC-анализа по заказам
├─ clients/
│ └─ wb.py # Клиент для запросов к API WB
├─ converters/
│ └─ abc.py # Конвертер из модели БД в модель ABCResult
├─ models/
│ └─ abc.py # Структура таблицы abc_analysis
├─ repositories/
│ └─ abc.py # Функции получения/записи ABC в БД
├─ schemas/
│ ├─ order.py # Pydantic модель заказа из API WB
│ └─ abc.py # Pydantic модель объекта ABC
├─ services/
│ ├─ abc_calculator.py # Сервис для расчёта ABC
│ └─ abc_service.py # Сервис получения существующего ABC или расчёт нового при отсутствии данных
├─ config.py # Загрузка переменных окружения
├─ database.py # Движок и подключение к БД
└─ main.py # Точка входа в приложение


---

## 🚀 Запуск проекта

### Docker
1. Создайте файл `.env.docker` и скопируйте в него содержимое `.env.example`.
2. Замените значение `API_KEY` на действительный ключ WB API.
3. Задайте параметры подключения к БД через переменные POSTGRES_USER, POSTGRES_PASSWORD.
4. Задайте значения переменных POSTGRES_HOST = db, POSTGRES_PORT = 5432
5. Сборка образа: docker-compose build backend
6. Запуск контейнера: docker-compose up -d


### uvicorn (dev)
1. Создайте базу данных PostgreSQL.
2. Создайте файл .env и скопируйте в него содержимое .env.example.
3. Замените значение API_KEY на действительный ключ WB API
4. Задайте параметры подключения к вашей БД через переменные 
   POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_HOST, POSTGRES_PORT, POSTGRES_DB
5. Создайте и активируйте виртуальное окружение.
6. Установите зависимости: pip install -r requirements.txt
7. Запуск приложения: uvicorn src.main:app --reload